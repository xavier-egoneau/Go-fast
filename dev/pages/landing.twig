<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Landing Page - Design System</title>
  <link rel="stylesheet" href="../assets/css/style.css">
</head>
<body>
  {% set hero_variant = hero_variant|default('primary') %}
  {% set show_features = show_features|default(true) %}
  {% set show_cta = show_cta|default(true) %}

  <!-- Navbar -->
  <nav class="navbar navbar--{{ hero_variant == 'dark' ? 'dark' : 'default' }}">
    <div class="container">
      <div class="navbar__content">
        <div class="navbar__brand">
          <span class="navbar__logo">🎨 Design System</span>
        </div>
        <ul class="navbar__menu">
          <li><a href="#features" class="navbar__link">Fonctionnalités</a></li>
          <li><a href="#about" class="navbar__link">À propos</a></li>
          <li><a href="#contact" class="navbar__link">Contact</a></li>
        </ul>
        <div class="navbar__actions">
          <button class="btn btn--{{ hero_variant == 'dark' ? 'primary' : 'secondary' }} btn--small">
            Se connecter
          </button>
        </div>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="hero hero--{{ hero_variant }}">
    <div class="container">
      <div class="hero__content">
        <h1 class="hero__title">
          {% if hero_variant == 'minimal' %}
            Design System Simple
          {% elseif hero_variant == 'dark' %}
            Créez des Interfaces Exceptionnelles
          {% else %}
            Bienvenue dans votre Design System
          {% endif %}
        </h1>
        <p class="hero__subtitle">
          {% if hero_variant == 'minimal' %}
            Des composants réutilisables pour vos projets.
          {% else %}
            Un ensemble de composants modulaires et réutilisables pour construire vos applications rapidement et efficacement.
          {% endif %}
        </p>
        {% if show_cta %}
        <div class="hero__actions">
          <button class="btn btn--primary btn--large">
            Commencer
          </button>
          <button class="btn btn--outline btn--large">
            En savoir plus
          </button>
        </div>
        {% endif %}
      </div>
    </div>
  </section>

  <!-- Features Section -->
  {% if show_features %}
  <section class="features" id="features">
    <div class="container">
      <h2 class="features__title">Nos Fonctionnalités</h2>
      <p class="features__subtitle">Tout ce dont vous avez besoin pour construire de belles interfaces</p>

      <div class="features__grid">
        <div class="card">
          <div class="card__header">
            <h3 class="card__title">🎨 Composants Modulaires</h3>
          </div>
          <div class="card__body">
            <p>Des composants réutilisables et personnalisables pour tous vos besoins.</p>
          </div>
        </div>

        <div class="card">
          <div class="card__header">
            <h3 class="card__title">⚡ Performance</h3>
          </div>
          <div class="card__body">
            <p>Optimisé pour la vitesse et la légèreté, sans compromis sur les fonctionnalités.</p>
          </div>
        </div>

        <div class="card">
          <div class="card__header">
            <h3 class="card__title">📱 Responsive</h3>
          </div>
          <div class="card__body">
            <p>Parfaitement adapté à tous les écrans, du mobile au desktop.</p>
          </div>
        </div>

        <div class="card">
          <div class="card__header">
            <h3 class="card__title">🔧 Personnalisable</h3>
          </div>
          <div class="card__body">
            <p>Adaptez facilement les composants à votre charte graphique.</p>
          </div>
        </div>
      </div>
    </div>
  </section>
  {% endif %}

  <!-- CTA Section -->
  {% if show_cta %}
  <section class="cta">
    <div class="container">
      <div class="cta__content">
        <h2 class="cta__title">Prêt à commencer ?</h2>
        <p class="cta__subtitle">Rejoignez des milliers de développeurs qui utilisent notre Design System</p>
        <button class="btn btn--primary btn--large">
          Démarrer gratuitement
        </button>
      </div>
    </div>
  </section>
  {% endif %}

  <!-- Footer -->
  <footer class="footer">
    <div class="container">
      <p>&copy; 2024 Design System. Tous droits réservés.</p>
    </div>
  </footer>

  <script>
    // Gestion des variantes via paramètres URL
    (function() {
      const urlParams = new URLSearchParams(window.location.search);

      // Hero variant
      const heroVariant = urlParams.get('hero_variant');
      if (heroVariant) {
        const hero = document.querySelector('.hero');
        hero.className = `hero hero--${heroVariant}`;

        // Mettre à jour la navbar selon le variant
        const navbar = document.querySelector('.navbar');
        if (heroVariant === 'dark') {
          navbar.className = 'navbar navbar--dark';
          navbar.querySelector('.btn').className = 'btn btn--primary btn--small';
        }

        // Mettre à jour les titres selon le variant
        const title = document.querySelector('.hero__title');
        const subtitle = document.querySelector('.hero__subtitle');

        if (heroVariant === 'minimal') {
          title.textContent = 'Design System Simple';
          subtitle.textContent = 'Des composants réutilisables pour vos projets.';
        } else if (heroVariant === 'dark') {
          title.textContent = 'Créez des Interfaces Exceptionnelles';
        }
      }

      // Show/Hide features
      if (urlParams.get('show_features') === 'false') {
        const features = document.querySelector('.features');
        if (features) features.style.display = 'none';
      }

      // Show/Hide CTA
      if (urlParams.get('show_cta') === 'false') {
        const cta = document.querySelector('.cta');
        if (cta) cta.style.display = 'none';
        const heroActions = document.querySelector('.hero__actions');
        if (heroActions) heroActions.style.display = 'none';
      }
    })();
  </script>
</body>
</html>
