{# Badge Component #}
{% set variant = variant|default('primary') %}
{% set size = size|default('normal') %}
{% set rounded = rounded|default(false) %}
{% set text = text|default('Badge') %}

<span class="badge badge--{{ variant }} badge--{{ size }}{% if rounded %} badge--rounded{% endif %}">
  {{ text }}
</span>
